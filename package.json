{
  "name": "mern-task-manager",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "client-build": "cd client && npm run build",
    "build": "npm run client-build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["mern", "mongodb", "express", "react", "nodejs", "tasks"],
  "author": "",
  "license": "ISC",
  "description": "A MERN stack task management application with user authentication",
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
